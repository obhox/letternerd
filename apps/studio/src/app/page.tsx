/**
 * Placeholder root. Replaced by the site picker once memberships land.
 */
export default function Page() {
  return (
    <main className="mx-auto max-w-2xl p-16">
      <h1 className="text-2xl font-semibold">CMS Studio</h1>
      <p className="mt-2 text-[var(--color-ink-muted)]">
        Multi-site content, served headlessly to each site&rsquo;s own domain.
      </p>
    </main>
  );
}
