node_modules/
.next/
dist/
out/
.turbo/
*.tsbuildinfo

.env
.env.local
.env.*.local

.DS_Store
*.log
coverage/
.vitest/

# local object storage / db volumes
infra/.data/

# Next loads dotenv from the app dir, not the repo root; this is a symlink to ../../.env
apps/studio/.env

# timestamped .env backups taken before config edits
.env.backup-*
